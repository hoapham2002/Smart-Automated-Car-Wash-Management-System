spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres?user=postgres.ewmydwhahuxhkywoivkk&password=Abcdef123456789,,,

    username: postgres.ksmnsefgguukmfzdadcd

    password: 'Abcdef123456789,,,'

    driver-class-name: org.postgresql.Driver

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisRepositoriesAutoConfiguration
      - org.springframework.boot.autoconfigure.data.redis.RedisReactiveAutoConfiguration
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
management:
  health:
    redis:
      enabled: false
logging:
  level:
    com.autowash: DEBUG
    org.flywaydb: INFO
    org.springframework.boot.autoconfigure.flyway: DEBUG

#  d6d0faa6-f06b-4935-99ab-78117de3f75a
cors:
  allowed-origins: ${ALLOWED_ORIGINS:http://localhost:5173}
